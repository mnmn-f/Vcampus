# 2026-09-16 本地与 GitHub 整合记录

- 本地修复先保存为 `92a4425`，本地安全分支为 `safety/pre-sync-20260916`。
- 合入远程 `main` 至 `28a7da8`：图书馆界面、公告详情及 AI 路由改进。
- 合入 PR #7（`ae6d704`）：宿舍床位图、分页、审批、公告及人员显示修复。
- 冲突保留双方业务功能；保留本地输入限制、去冗余说明和标准按钮尺寸。图书馆时间下拉框不再调用文本输入过滤器，封面按钮不随表单拉伸。
- 本地 V21 自然演示数据与远程教师权限脚本重号，后者调整为 `V26__teacher_library_access.sql`；已有数据库只需补未执行脚本，不重建数据库。
- PR 附带的 `vcampus-backup.sql` 不纳入合并后的源码树；本机配置、临时文件和日志不上传。PR 历史中的备份未做历史重写。
- 整合精简版软件设计说明书位于 `docs/reports/`，学籍章节尚待提交。

普通 `mvnw test` 已通过，包含客户端多角色界面检查、输入约束、商店及宿舍回归、服务端事务与权限测试。需要外部环境或显式开关的测试维持跳过；此记录不代表重新执行了所有真实 MySQL 集成测试。未执行数据库导入或修复数据脚本。
