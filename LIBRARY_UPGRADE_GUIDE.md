# 图书馆六项修改交付说明

本版本基于你上传的 `Vcampus-main(1).zip` 修改。完整源码、客户端/服务端 JAR、SQL 升级脚本和测试均在本项目中。保留 Java 17、现有 Maven 依赖版本、Swing 主题和原有业务入口。

## 一、按六项计划完成的修改

| 计划 | 实现结果 |
| --- | --- |
| 页面适配和日历 | 预约表单随宽度自动变为 4 / 2 / 1 列；日历选日期，过去日期不可选；保留时间与冲突校验。窄窗口时图书馆菜单移至上方，表单换行、页面纵向滚动，多列表格内部横向滚动。 |
| 书籍详情 | 列表“查看详情”打开独立详情窗口，包含封面占位图/真实封面、书名、作者、出版社、年份、ISBN、分类、位置、库存、简介和本人相关借阅记录。可在详情借阅，并同步刷新列表、库存和个人记录。 |
| 学生线上资源 | “线上资源 → PDF 文件资源”提供公共列表、资源详情、上传、我的上传、下载任务与历史。可勾选多个 PDF 下载；最多两个任务并行，其余排队。 |
| 管理员审核 | “线上资源管理 → PDF 文件资源”提供待审核和全部 PDF；可下载并用系统 PDF 阅读器查看，再通过或拒绝。拒绝理由必填，学生端同步显示；支持停用已通过资源。 |
| 数据与文件存储 | 新增 MySQL 元数据/下载历史表，PDF 放在服务端持久目录；新增图书出版年份和封面字段。Demo 的 PDF 文件和记录也保存在本机，重开后仍可使用。 |
| 联调验收 | 增加真实 TCP 大文件往返、审核与跨账号鉴权、分块连接轮换、并行同名下载、持久记录、元数据保留和窄窗口布局测试。具体结果见 `LIBRARY_VALIDATION.md`。 |

原有线上链接、链接访问日志、图书借还、自习室预约和管理员管理入口继续可用。管理员借阅管理没有新增“归还”按钮。

## 二、直接运行演示版（Windows）

1. 把压缩包完整解压到一个新文件夹，在 `Vcampus-main` 根目录打开 PowerShell。
2. 使用 Java 17 或更高版本运行已附带的客户端 JAR：

```powershell
java "-Dvcampus.client.mode=demo" -jar ".\vcampus-client\target\vCampusClient.jar"
```

也可以使用原有脚本：

```powershell
.\scripts\start-library-demo.ps1
```

| 角色 | 账号 | 密码 |
| --- | --- | --- |
| 学生 | demo_student | student123 |
| 图书管理员 | demo_librarian | library123 |

建议在同一客户端中先用学生上传，再退出登录切换图书管理员审核，然后切回学生查看。

Demo PDF 数据保存在 `%USERPROFILE%\.vcampus\library-pdf-demo`，包括 `records.bin` 和 `files`。图书借阅、自习室等原有 Demo 数据仍按原项目规则在内存中运行；多电脑、多客户端协作请使用下面的网络模式。

## 三、网络模式数据库升级

### 已有数据库

新版服务端启动时会自动检查并补齐图书年份、封面字段、PDF 表，并写入 9 本带封面的演示图书；重复启动不会重复插入。如果数据库账号没有 `ALTER`、`CREATE` 和 `INSERT` 权限，可在备份后由数据库管理员手工执行：

```
vcampus-server/src/main/resources/db/migration/V6__library_pdf_and_book_details.sql
```

此脚本只增加图书字段和 PDF 表，不删除旧图书、借阅、预约或线上链接，并且可以重复执行。

### 全新数据库

按原项目 README 导入 `V1__baseline.sql`、`V2__demo_data.sql` 及所需其他模块升级脚本。启动新版服务端时会自动完成本次图书馆升级，也可以手工执行 `V6__library_pdf_and_book_details.sql`。

如果不希望服务端自动升级数据库，可增加 JVM 参数 `-Dvcampus.library.schema.auto-upgrade=false`，此时必须先手工执行 V6。

### 启动

按原项目数据库参数和脚本启动新服务端，然后启动新客户端：

```powershell
.\scripts\start-server.ps1
```

在另一个 PowerShell 窗口中：

```powershell
.\scripts\start-client.ps1
```

客户端和服务端都要使用本压缩包中的新版本，不能混用旧 JAR。网络模式账号使用你数据库中的实际账号；上面的 `demo_*` 账号仅用于演示模式。

PDF 默认放在**服务端工作目录下的 `data/library-pdf`**。请从固定的项目根目录启动服务端。也可在服务端 Java 命令中增加 `-Dvcampus.library.files=D:\VcampusData\library-pdf` 指定固定目录。需要迁移或备份系统时，同时保留数据库和此文件夹。

## 四、重新构建源码

在包含 `pom.xml` 的项目根目录执行：

```powershell
.\mvnw.cmd clean package
```

构建产物：

- `vcampus-client/target/vCampusClient.jar`
- `vcampus-server/target/vCampusServer.jar`

单独运行图书馆相关测试：

```powershell
.\mvnw.cmd -pl vcampus-client,vcampus-server -am "-Dtest=*Library*,*Pdf*,BookDetailsMetadataTest" "-Dsurefire.failIfNoSpecifiedTests=false" test
```

无需修改 `pom.xml`、JDK 配置、依赖版本或全局主题文件。本次环境的临时构建代理设置没有放进项目。

## 五、操作和状态规则

- PDF 单文件最大 50 MB；传输使用 128 KB 分块。提交前检查文件名、大小、PDF 标识和 SHA-256，完整传输后才进入审核列表。
- 所有上传先为“待审核”。只有“已通过”资源进入公共列表。
- 学生可查看、下载自己的未公开文件；管理员可查看待审核文件。其他学生即使知道资源编号，也无法通过详情/下载接口访问。
- 审核结果每 15 秒自动刷新，也可以手动刷新；自动刷新保留已选行、勾选和查询条件。
- 拒绝理由最多 1000 字且不能为空。“我的上传”列表和详情均可查看。
- 已通过的 PDF 内容不可原地替换。修改文件时重新上传，生成新的待审核记录，重新审核后公开。
- 下载进度基于实际传输字节。完成后核对 SHA-256，再保存最终文件；同名文件自动编号，不覆盖已有文件。
- 下载失败可在任务列表重试，历史记录也可重新下载。重试从头开始，不提供跨重启断点续传。
- 关闭客户端会中断尚未完成的任务；已完成历史记录保留。网络中断时可能保留“未完成”记录，可重新下载。
- 退出登录后，旧 PDF 操作不能借用新登录用户的身份继续请求。
- 图书封面可选择 PNG/JPG，客户端生成适合展示的缩略图后随书目保存；缺少封面和年份的旧书仍能正常显示。

## 六、手工验收建议

1. 在小窗口打开“自习室预约”，确认日期、开始时间、结束时间和提交按钮都能使用。
2. 管理员维护一本书的封面、年份和简介；学生打开详情借阅，检查库存和个人记录刷新。
3. 学生 A 上传 PDF；学生 B 搜索不到它，也不能下载未审核文件。
4. 管理员拒绝一次并填写理由；学生 A 在“我的上传”查看理由。
5. 学生 A 重新上传；管理员下载检查并审核通过；学生 B 可搜索、查看详情并下载。
6. 勾选多个 PDF，检查两个后台任务、排队、真实进度、同名文件编号和下载记录。
7. 关闭并重开客户端，确认上传状态和下载历史仍保留。网络模式另需重开服务端检查数据库与文件目录的持久化。

## 附：一处原项目构建稳定性修复

完整测试发现原有商店内存账户仓储只按创建时间排序，同一毫秒产生付款和退款流水时顺序不稳定。已补上“时间相同按记录编号倒序”的规则，与原 MySQL 查询保持一致。没有变更付款、退款金额或其他商店业务规则。
